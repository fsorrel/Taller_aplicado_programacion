# Matriz revisada de atributos - EcoConce

Fuente: ecoconce_modificado_usuario_reciclo (1).ddl

Este archivo se agregó para validar que el backend/front no trabaje con DTOs resumidos cuando se quiere revisar la base completa.

Total tablas detectadas: **18**

## comunas (3 columnas)

| Columna | Definición Oracle |
|---|---|
| `id` | `NUMBER GENERATED BY DEFAULT AS IDENTITY` |
| `nombre` | `VARCHAR2 (100)  NOT NULL` |
| `region_id` | `NUMBER  NOT NULL` |

## detalle_formulario_materiales (7 columnas)

| Columna | Definición Oracle |
|---|---|
| `id` | `NUMBER GENERATED BY DEFAULT AS IDENTITY` |
| `formulario_id` | `NUMBER  NOT NULL` |
| `material_id` | `NUMBER  NOT NULL` |
| `cantidad_declarada` | `NUMBER  NOT NULL` |
| `unidad_declarada` | `VARCHAR2 (30) DEFAULT 'UNIDAD'  NOT NULL` |
| `puntos_obtenidos` | `NUMBER DEFAULT 0  NOT NULL` |
| `observacion` | `CLOB` |

## estado_punto (2 columnas)

| Columna | Definición Oracle |
|---|---|
| `id` | `NUMBER GENERATED BY DEFAULT AS IDENTITY` |
| `nombre` | `VARCHAR2 (50)  NOT NULL` |

## formularios_reciclaje (8 columnas)

| Columna | Definición Oracle |
|---|---|
| `id` | `NUMBER GENERATED BY DEFAULT AS IDENTITY` |
| `usuario_id` | `NUMBER  NOT NULL` |
| `punto_id` | `NUMBER  NOT NULL` |
| `distancia_metros` | `NUMBER (6,2)  NOT NULL` |
| `total_puntos_obtenidos` | `NUMBER DEFAULT 0  NOT NULL` |
| `estado` | `VARCHAR2 (20) DEFAULT 'PENDIENTE'  NOT NULL` |
| `observacion` | `CLOB` |
| `fecha_formulario` | `TIMESTAMP DEFAULT CURRENT_TIMESTAMP  NOT NULL` |

## guias_reciclaje (6 columnas)

| Columna | Definición Oracle |
|---|---|
| `id` | `NUMBER GENERATED BY DEFAULT AS IDENTITY` |
| `titulo` | `VARCHAR2 (150)  NOT NULL` |
| `descripcion` | `CLOB  NOT NULL` |
| `contenido` | `CLOB  NOT NULL` |
| `material_id` | `NUMBER` |
| `fecha_creacion` | `TIMESTAMP DEFAULT CURRENT_TIMESTAMP` |

## historial_estado_puntos (6 columnas)

| Columna | Definición Oracle |
|---|---|
| `id` | `NUMBER GENERATED BY DEFAULT AS IDENTITY` |
| `punto_id` | `NUMBER  NOT NULL` |
| `estado_anterior_id` | `NUMBER` |
| `estado_nuevo_id` | `NUMBER` |
| `descripcion` | `CLOB` |
| `fecha_historial` | `TIMESTAMP DEFAULT CURRENT_TIMESTAMP` |

## historial_premios_canjeados (10 columnas)

| Columna | Definición Oracle |
|---|---|
| `id` | `NUMBER GENERATED BY DEFAULT AS IDENTITY` |
| `usuario_id` | `NUMBER  NOT NULL` |
| `premio_id` | `NUMBER  NOT NULL` |
| `nombre_premio` | `VARCHAR2 (120)  NOT NULL` |
| `puntos_gastados` | `NUMBER  NOT NULL` |
| `codigo_canje` | `VARCHAR2 (80)` |
| `estado` | `VARCHAR2 (20) DEFAULT 'PENDIENTE'  NOT NULL` |
| `fecha_canje` | `TIMESTAMP DEFAULT CURRENT_TIMESTAMP` |
| `fecha_entrega` | `TIMESTAMP` |
| `observacion` | `CLOB` |

## historial_recolecciones_puntos (7 columnas)

| Columna | Definición Oracle |
|---|---|
| `id` | `NUMBER GENERATED BY DEFAULT AS IDENTITY` |
| `punto_id` | `NUMBER  NOT NULL` |
| `material_id` | `NUMBER  NOT NULL` |
| `mantenedor_id` | `NUMBER` |
| `cantidad_retirada_compactada` | `NUMBER  NOT NULL` |
| `observacion` | `CLOB` |
| `fecha_recoleccion` | `TIMESTAMP DEFAULT CURRENT_TIMESTAMP` |

## materiales (4 columnas)

| Columna | Definición Oracle |
|---|---|
| `id` | `NUMBER GENERATED BY DEFAULT AS IDENTITY` |
| `nombre` | `VARCHAR2 (100)  NOT NULL` |
| `codigo_identificador` | `VARCHAR2 (100)  NOT NULL` |
| `descripcion` | `CLOB  NOT NULL` |

## movimientos_puntos_usuario (8 columnas)

| Columna | Definición Oracle |
|---|---|
| `id` | `NUMBER GENERATED BY DEFAULT AS IDENTITY` |
| `usuario_id` | `NUMBER  NOT NULL` |
| `tipo_movimiento` | `VARCHAR2 (20)  NOT NULL` |
| `puntos` | `NUMBER  NOT NULL` |
| `formulario_id` | `NUMBER` |
| `canje_id` | `NUMBER` |
| `descripcion` | `CLOB` |
| `fecha_movimiento` | `TIMESTAMP DEFAULT CURRENT_TIMESTAMP` |

## premios (6 columnas)

| Columna | Definición Oracle |
|---|---|
| `id` | `NUMBER GENERATED BY DEFAULT AS IDENTITY` |
| `nombre` | `VARCHAR2 (120)  NOT NULL` |
| `descripcion` | `CLOB  NOT NULL` |
| `costo_puntos` | `NUMBER  NOT NULL` |
| `stock` | `NUMBER DEFAULT 0  NOT NULL` |
| `activo` | `CHAR (1) DEFAULT 'S'  NOT NULL` |

## punto_materiales (5 columnas)

| Columna | Definición Oracle |
|---|---|
| `id` | `NUMBER GENERATED BY DEFAULT AS IDENTITY` |
| `punto_id` | `NUMBER  NOT NULL` |
| `material_id` | `NUMBER  NOT NULL` |
| `capacidad_compactado` | `NUMBER DEFAULT 0  NOT NULL` |
| `actual_compactado` | `NUMBER DEFAULT 0  NOT NULL` |

## puntos_reciclaje (12 columnas)

| Columna | Definición Oracle |
|---|---|
| `id` | `NUMBER GENERATED BY DEFAULT AS IDENTITY` |
| `nombre` | `VARCHAR2 (120)  NOT NULL` |
| `descripcion` | `CLOB  NOT NULL` |
| `comuna_id` | `NUMBER  NOT NULL` |
| `direccion` | `VARCHAR2 (200)` |
| `latitud` | `NUMBER (10,7)  NOT NULL` |
| `longitud` | `NUMBER (10,7)  NOT NULL` |
| `radio_validacion_m` | `NUMBER DEFAULT 50  NOT NULL` |
| `estado_id` | `NUMBER  NOT NULL` |
| `mantenedor_id` | `NUMBER` |
| `fecha_creacion` | `TIMESTAMP DEFAULT CURRENT_TIMESTAMP` |
| `ultima_actualizacion` | `TIMESTAMP DEFAULT CURRENT_TIMESTAMP` |

## regiones (2 columnas)

| Columna | Definición Oracle |
|---|---|
| `id` | `NUMBER GENERATED BY DEFAULT AS IDENTITY` |
| `nombre` | `VARCHAR2 (100)  NOT NULL` |

## reportes_puntos (6 columnas)

| Columna | Definición Oracle |
|---|---|
| `id` | `NUMBER GENERATED BY DEFAULT AS IDENTITY` |
| `usuario_id` | `NUMBER  NOT NULL` |
| `punto_id` | `NUMBER  NOT NULL` |
| `tipo_reporte_id` | `NUMBER  NOT NULL` |
| `descripcion` | `CLOB` |
| `fecha_reporte` | `TIMESTAMP DEFAULT CURRENT_TIMESTAMP` |

## roles (2 columnas)

| Columna | Definición Oracle |
|---|---|
| `id` | `NUMBER GENERATED BY DEFAULT AS IDENTITY` |
| `nombre` | `VARCHAR2 (50)  NOT NULL` |

## tipo_reporte (2 columnas)

| Columna | Definición Oracle |
|---|---|
| `id` | `NUMBER GENERATED BY DEFAULT AS IDENTITY` |
| `nombre` | `VARCHAR2 (50)  NOT NULL` |

## usuarios (15 columnas)

| Columna | Definición Oracle |
|---|---|
| `id` | `NUMBER GENERATED BY DEFAULT AS IDENTITY` |
| `rut` | `VARCHAR2 (12)  NOT NULL` |
| `nombre_alias` | `VARCHAR2 (100)  NOT NULL` |
| `correo` | `VARCHAR2 (120)  NOT NULL` |
| `contrasena` | `VARCHAR2 (255)  NOT NULL` |
| `sexo_genero` | `VARCHAR2 (50)` |
| `fecha_nacimiento` | `DATE` |
| `telefono` | `VARCHAR2 (20)` |
| `comuna_id` | `NUMBER  NOT NULL` |
| `direccion` | `VARCHAR2 (200)` |
| `puntos` | `NUMBER DEFAULT 0  NOT NULL` |
| `rol_id` | `NUMBER  NOT NULL` |
| `activo` | `CHAR (1) DEFAULT 'S'  NOT NULL` |
| `fecha_registro` | `TIMESTAMP DEFAULT CURRENT_TIMESTAMP` |
| `fecha_ultimo_acceso` | `TIMESTAMP` |
